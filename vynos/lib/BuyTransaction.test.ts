import BuyTransaction from './BuyTransaction'
import { WorkerState, INITIAL_STATE } from '../worker/WorkerState'
import * as redux from 'redux'
import {Store} from 'redux'
import reducers from '../worker/reducers'
import { expect } from 'chai'

describe('class DepositTransaction implements TransactionInterface', () => {
  it(`
    Should open a new vc if tip is larger than current vc can handle
  `)
  
  it('Should call this.connext.updateBalance to update balance')

})